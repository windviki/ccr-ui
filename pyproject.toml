[project]
name = "ccr-ui"
version = "0.1.0"
description = "轻量级 Claude Code Router (CCR) 可视化配置前端（模型选择 / Provider 增删改）"
readme = "README.md"
license = "MIT"
authors = [{ name = "viki", email = "windviki@gmail.com" }]
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
