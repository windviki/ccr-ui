# ccr-ui 环境变量示例。
# 复制为 `.env` 后按需修改（`.env` 已被 .gitignore 忽略，不会入库）。
# 优先级：命令行参数 > 环境变量 / .env > 默认值。

# ---- UI 监听 ----
# 监听地址 / 端口（对应 --host / --port）
#CCR_UI_HOST=127.0.0.1
#CCR_UI_PORT=24678

# ---- CCR 连接 ----
# CCR 管理 RPC 端点（对应 --ccr-base）
#CCR_RPC_URL=http://127.0.0.1:3458/api/ccr/rpc
# CCR service.json 位置（含 web token），`~` 自动展开为用户主目录
#CCR_SERVICE_FILE=~/.claude-code-router/service.json

# ---- 访问口令（可选）----
# 固定 UI 访问口令；留空则自动从 service.json 的 url 参数读取 ccr_web_token。
# 该口令同时用于 CCR RPC 的 x-ccr-web-auth 请求头。
#CCR_UI_TOKEN=

# ---- code-server 反向代理（可选）----
# 若通过 code-server 代理访问，填其基址（不含 /proxy/<port> 后缀），
# 启动日志会打印带口令的完整访问链接；留空则只打印本机地址。
#CODE_SERVER_BASE_URL=https://code.example.com
